on:
  push:
    branches:
      - wildcard
    paths:
      - "docs/**/*"
      - "tests/**/*"
      - "mkdocs.yml"

jobs:
  wildcard:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: |
          true
